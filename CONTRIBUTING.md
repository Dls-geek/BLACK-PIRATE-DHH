# Contributing

This is a personal scratch lab for small language experiments.

- Keep each file self-contained — one mixer per file.
- Match the language folder: `src/python`, `src/rust`, etc.
- MIT license applies to new files too.
